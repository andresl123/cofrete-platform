# CI Scripts

This directory is reserved for helper scripts used by GitHub Actions.

Planned script:

- `docker_publish_matrix.py`

CI helpers must not print secrets or assume production deployment from pull requests.
