# Web App Agent Guide

Owns the web client application. Keep web source, tests, assets, and local frontend configuration in this directory unless a task explicitly introduces shared contracts or documentation.

Run the root harness check after repository-structure changes:

```sh
python ../scripts/agent_harness_check.py
```
